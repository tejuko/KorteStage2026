# Daglog — 02/06/2026 (dinsdag)

## Wat heb ik gedaan?
**Ontwerp dynamische SEO-pagina gepresenteerd**
- Mijn ontwerp/uitwerking voor de dynamische SEO-pagina laten zien aan Danique en Conrad.
- Goede feedback gekregen — fijne bevestiging dat de richting klopt. 😎

**Bruidsmode Haarlem — SEA-landingspagina: feedback verwerkt** (`/sea-paservaring/`, pagina-ID 1757, template `page-sea.php`)
Alle 8 feedbackpunten doorgevoerd, live geverifieerd in de gerenderde HTML en caches geleegd.

_Desktop_
1. Afstand h3 ↔ alinea in "Wat maakt ons uniek" verkleind. Bleek niet de margin maar de header-hoogte (ring-icoon 31×43 → ~42px); opgelost met `margin-bottom: -8px` op `.sea-uniek__usp-header` (`sea-uniek.php`).
2. Carousel-pijlen terug naar Figma-maat: `width: 93px`, `aspect-ratio: 93/18.74` (was 120px) (`sea-bruiden.php`).

_Mobile_
3. Summer-sale-banner verborgen (alleen op deze pagina + mobile): `.page-template-page-sea .summer-sale-banner-wrapper { display:none }` (`sea-hero.php`).
4. Zelfde h3 ↔ alinea fix, mobiel `margin-bottom: -6px` (kleinere regelhoogte) (`sea-uniek.php`).
5. Spacing breadcrumbs → rating = 34px via `sea-hero` `padding-top: 34px` (`sea-hero.php`).
6. Spacing `nav.navigation` → breadcrumbs = 24px (oude theme-margin van 25px afgezet, `padding-top: 24px`) (`sea-hero.php`).
7. Helpen-foto uitgezoomd (twee modellen) — eigen class `sea-th__card--image-helpen` met `aspect-ratio: 1600/1088` zodat hoofden/handen niet afgesneden worden (`sea-twijfel-helpen.php`).
8. Pijlen mobile breedte 74.28px op beide breakpoints (768px/480px) (`sea-bruiden.php`).

**To do genoteerd**
- Video's weghalen op `home-dark` (`/home-dark/`).

## Wat heb ik geleerd?
- Een spacing-probleem zit niet altijd in de margin: hier zat de "te grote afstand" verstopt in de hoogte van een icoon-header. Eerst de echte oorzaak opsporen (computed box) loont vóór je margins gaat trimmen.
- Op een SEA-landingspagina mag je site-brede elementen (summer-sale-banner) lokaal en per breakpoint uitschakelen zonder de rest van de site te raken via een page-template-scoped selector.

## Feedback ontvangen (van wie? waarover? wat doe ik ermee?)
- **Danique + Conrad** — positieve feedback op mijn ontwerp voor de dynamische SEO-pagina. ✅ richting bevestigd, ga ermee verder.
- **Bruidsmode Haarlem (reviewronde)** — 8 concrete punten op de SEA-landingspagina, allemaal verwerkt en live geverifieerd.

## Vragen / dingen waar ik tegenaan liep
- Mobiele pijl-hoogte: ik kreeg 74.28×2.25px door. Breedte exact gezet, maar de natuurlijke verhouding aangehouden — 2.25px zou de pijlpunt platdrukken (vermoedelijk de líjndikte in Figma). Open: als de letterlijke versie tóch moet, SVG-export opvragen.
- De page-HTML met inline `<style>`-blokken werd wél gecachet (RUCSS staat hier uit) → na elke deploy WP cache flush + WP Rocket domain-cache + opcache legen.

## Voor/na — iteraties op mijn werk
- "Wat maakt ons uniek": ruime h3↔alinea-afstand → strak (header-hoogte gecompenseerd).
- Carousel-pijlen: 120px → Figma-maat 93px (desktop) / 74.28px (mobile).
- Helpen-foto: koppen/handen afgesneden → uitgezoomd met eigen aspect-ratio.

## Koppeling aan leerdoelen
- [x] 1. Samen ontwerpen — ontwerp dynamische SEO-pagina voorgelegd aan Danique + Conrad; review-feedback Bruidsmode verwerkt.
- [ ] 2. Oriënteren en begrijpen
- [x] 3. Verbeelden en conceptualiseren — eigen ontwerp dynamische SEO-pagina gepresenteerd.
- [x] 4. Prototypen en uitwerken — 8 feedbackpunten op de SEA-landingspagina geïmplementeerd.
- [x] 5. Evalueren — alle wijzigingen live geverifieerd in gerenderde HTML, backups (`.bak-20260602-072730`) + caches geleegd.
